@AGENTS.md

Claude Code loads this file automatically. `AGENTS.md` is the canonical
project instruction set shared with other coding assistants. Skills and
per-language rules live under `.agents/` and are exposed here as
`.claude/skills` and `.claude/rules`.
